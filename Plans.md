# 灵机剪影 — 开发计划
## 其他说明和规划
1.用到流remotion渲染视频，这里增加个人化
remotion可以通过代码生成视频，但是简单的视频

2.图片转换视频：
- 未来计划，导入图片后，可以剪辑成连贯的视频，
如何通过几张图片，生成一短流畅的视频故事和视频。

3.通过剧本生成对应严格详细的分割点计划。


## 项目主链路

```text
项目目录
  → original.md / script.md
  → TTS 音频 + SRT（MiniMax / Edge TTS）
  → AI 字幕分析 / 封面 / 信息卡 / Motion Card
  → 时间线编辑
  → Remotion 导出 H.264 MP4
```

---

## 一、短期任务（近 1-2 轮迭代）

### 1.1 视频创作者水印

**目标**：用户在导出视频时叠加创作者 SVG 水印。

- 设置页新增水印配置（SVG 上传 / 文字水印 + 位置 + 透明度）
- 持久化到 `AISettings` 或导出配置
- Remotion 导出链路叠加水印图层
- 预览中可见水印效果

### 1.2 视频创作日期

**目标**：在导出视频中显示创作日期。

- 日期格式配置（年月日 / 年月 / 仅年）
- 日期位置 / 字体 / 大小配置
- Remotion 合成中叠加日期文字层

### 1.3 布局结构调整（三栏布局）

**目标**：当前主内容区为左右两栏，改为左中右三栏。

```
┌──────────┬─────────────┬──────────┐
│ 素材区   │  播放器      │ 配置面板 │
│          │              │          │
│ 文件树   │  时间线      │ Inspector │
│ 素材库   │  预览        │ AI 面板  │
└──────────┴─────────────┴──────────┘
```

- `Editor` 页面布局重构为三栏 CSS Grid
- 左侧：素材面板（文件树 + 素材库）
- 中间：播放器 + 时间线
- 右侧：Inspector + AI 面板 + 导出
- 可拖拽调整栏宽

---

## 二、中期任务（2-4 轮迭代）

### 2.1 AI 写稿前置全流程

**目标**：从素材到成稿的全自动流。

```
抖音链接 → 视频下载 → 文案提取
  → 洗稿 / 改写 → 成稿（script.md）
  → TTS 生成 → AI 分析 → 导出
```

子任务分解：

- 抖音视频下载模块（已有导入入口，完善下载 & 重试）
- 文案提取（ASR / OCR / 下载已有字幕）
- 洗稿引擎（AI 改写 + 风格模板切换）
- 全流程编排：用户输入链接 → 一键出片

### 2.2 TTS 能力增强

- **多段落分角色 TTS**：不同段落指定不同音色（对话场景）
- **Edge TTS 异步导出**：在统一进度系统中展示 Edge TTS 生成进度
- **SSML 支持**：允许用户添加 SSML 标签控制发音、停顿

### 2.3 字幕编辑器增强

- 字幕样式预设管理（保存 / 加载 / 导出）
- 逐字对齐（基于 WordBoundary 时间戳修正）
- 双语字幕支持（原文 + 翻译）

---

## 三、长期任务（视用户反馈调整优先级）

### 3.1 多媒体平台发布

- **抖音 Agent 一键发布**：通过 Agent / MCP 对接抖音开放平台
- **小红书一键发布**：对接小红书开放 API
- 发布队列管理（定时发布、多平台同步）

### 3.2 Remotion 导出增强

- **多格式导出**：H.264 / H.265 / ProRes / GIF
- **自定义导出模板**：用户可选择或创建导出预设（分辨率、码率、帧率）
- **导出队列**：批量导出多个项目

### 3.3 多语言 / 国际化

- UI 国际化（中文 / 英文 界面）
- 多语言 TTS 自动检测（中文内容用中文音色，英文内容用英文音色）
- 字幕翻译：AI 翻译 + 人工校正

### 3.4 性能与稳定性

- **大型项目分片加载**：时间线轨道超过 50 条时启用虚拟列表
- **Remotion 渲染进度精确反馈**：当前 2%-30% 为估算心跳，改 realtime 回传
- **崩溃恢复**：项目文件自动备份 + 崩溃后恢复提示

---

## 四、技术债务与架构改进

### 4.1 测试覆盖

| 模块 | 当前 | 目标 |
|------|------|------|
| TTS 生成（含 Edge TTS） | 有 minimax-tts 测试 | 补充 edge-tts 后端测试 |
| 设置页 | 有 tts-config-tab 骨架测试 | 补全各 provider 交互测试 |
| IPC 三件套 | 部分覆盖 | main/preload/electron-api 同步验证 |

### 4.2 代码质量

- 类型 `AISettings` 字段持续增长，考虑拆分 `TTSSettings` 子类型
- `electron/main.ts` 体积不断增大，考虑按领域拆分 IPC handler 到独立文件
- `useAIVideoWorkflow.ts` 阶段逻辑已达 ~1200 行，考虑子模块化

### 4.3 构建与 CI

- 配置 TypeScript `strict` 模式并修复存量问题
- 接入 ESLint + Prettier
- GitHub Actions 自动化测试 + 构建验证

---

## 五、依赖升级跟踪

| 依赖 | 当前版本 | 关注点 |
|------|----------|--------|
| Electron | 41 | 跟踪 Chromium 安全更新 |
| Remotion | 4 | 新导出格式、性能优化 |
| edge-tts-universal | ^1.4.0 | 新声线、WebSocket 协议变更 |
| TailwindCSS | 4 | 框架升级 |
| React | 19 | 新 API 适配 |
| TypeScript | 6 | strict 模式启用 |

---

## 优先级标记

- 🔴 P0 — 用户阻塞 / 核心链路断裂
- 🟡 P1 — 重要体验提升
- 🟢 P2 — 锦上添花 / 技术债务
- ⚪ P3 — 远期探索

首次排期建议优先推进 **1.1（水印）**、**1.3（三栏布局）** 和 **2.1（AI 写稿前置）**，这三个直接影响创作者核心体验。
